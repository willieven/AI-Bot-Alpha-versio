import os
import shutil
import logging

def list_directory(path):
    # Implementation for listing directory contents

def change_directory(session, path):
    # Implementation for changing directory

def make_directory(session, path):
    # Implementation for creating a directory

def remove_directory(session, path):
    # Implementation for removing a directory

def delete_file(session, path):
    # Implementation for deleting a file

def rename_file(session, from_path, to_path):
    # Implementation for renaming a file

def get_file_size(path):
    # Implementation for getting file size

def read_file(path, start_pos=0):
    # Implementation for reading a file

def write_file(path, data, mode='wb'):
    # Implementation for writing to a file